#!/usr/bin/env bash
# Свойства группы my-static.
# GROUP_ENABLED управляет вкл/выкл на уровне гита:
#   false — группа собирается в dist/, но НЕ получает !#include в главный список
#           (uBlock перестаёт применять её после следующего обновления),
#   true  — группа подключена к главному списку (значение по умолчанию).
export GROUP_ENABLED=true
